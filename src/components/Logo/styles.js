import { BackHandler, StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: "auto",
    backgroundColor: '#fff',
    borderRadius: 50,
    
  },
  logo: {
    marginTop: 90,
    display: "flex",
    left: 24
  },
  title: {
    color: "#132d46",
    fontSize: 30,
    left: 120,
    top: -75
  }
});

export default styles